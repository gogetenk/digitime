@if (string.IsNullOrEmpty(Value))
{
    <div class="overflow-hidden rounded-lg bg-white px-4 py-4 shadow-md sm:p-6">
        <div class="animate-pulse h-2 bg-gray-100 rounded-full dark:bg-gray-700 max-w-[100px] mb-2.5"></div>
        <div class="animate-pulse h-3 bg-gray-300 rounded-full dark:bg-gray-800 w-40 mb-4"></div>
    </div>
}
else
{
    <div class="overflow-hidden rounded-lg bg-white px-4 py-4 shadow-md sm:p-6">
        <dt class="truncate text-sm font-medium text-gray-500">@Title</dt>
        <dd class="mt-1 text-3xl font-semibold tracking-tight text-gray-900">
            @Value
        </dd>
    </div>
}

@code {
    [Parameter]
    public string Value { get; set; }

    [Parameter]
    public string Title { get; set; }
}